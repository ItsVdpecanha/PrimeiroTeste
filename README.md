# 🚀 Projeto de Testes E2E com Playwright

Este repositório contém exemplos de testes End-to-End (E2E) escritos em JavaScript utilizando a biblioteca Playwright.

---

## 🛠️ Tecnologias Utilizadas

* **Playwright:** Biblioteca para automação de testes E2E.
* **Node.js & npm:** Ambiente de execução e gerenciador de pacotes.

## ▶️ Como Rodar os Testes

1.  **Instalar Dependências:**
    ```bash
    npm install
    ```
2.  **Executar Todos os Testes:**
    ```bash
    npx playwright test
    ```
3.  **Abrir o Modo Interativo (UI Mode):**
    ```bash
    npx playwright test --ui
    ```
